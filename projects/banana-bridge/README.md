# Banana Bridge (Optional Spigot/Paper Plugin)

Banana Bridge is an **optional, separate project** from Banana Client.

Potential integration ideas:
- Client handshake/version reporting
- Server-side profile sync metadata
- Optional cosmetics synchronization
- Compatibility diagnostics command (`/bananabridge`)

This plugin does not bundle with the client and can be deployed independently on supported servers.
